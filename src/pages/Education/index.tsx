import { InfoPage } from '../../components';
import { educationContent } from '../../info/Contents';

export const EducationPage: React.FC = () => {
  return <InfoPage content={educationContent} />;
};
