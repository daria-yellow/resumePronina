import { InfoPage } from '../../components';
import { experienceContent } from '../../info/Contents';

export const ExperiencePage: React.FC = () => {
  return <InfoPage content={experienceContent} />;
};
